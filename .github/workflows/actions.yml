name: 'change'
on:
  issue_comment:
    types:
      - "created"
      - "edited"

jobs:
  change:
    runs-on: ubuntu-latest
    steps:
    - name: time logger
      run: |
        git config user.name github-actions
