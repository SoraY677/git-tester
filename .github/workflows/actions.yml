name: 'change'
on:
  issue_comment:
    types:
      - "created"
      - "edited"

jobs:
  change:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 1
    - name: time logger
      run: |
        git config user.name github-actions
