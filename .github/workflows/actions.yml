name: 'change'
on:
  push:
    paths:
      - 'sampleA/**'

jobs:
  change:
    runs-on: ubuntu-latest
    steps:
    - name: time logger
      run: |
        echo "hogehoge" > log.txt
        git config user.name github-actions
        git config user.email github-actions@github.com
        git add .
        git commit -m "Update"
        git push
