name: 'change'
on:
  push:
    paths:
      - 'sampleA/**'

jobs:
  change:
    runs-on: ubuntu-latest
    steps:
    - name: Add comment
      run: |
        echo "CURRENT_DATETIME=$(date +'%Y-%m-%d %H:%M:%S')" >> $GITHUB_ENV
